@model IEnumerable<string>

@{
    ViewBag.Title = "Subscriptions";
}

<h2>@ViewBag.SubscriptionsCount subscriptions</h2>

<div class="row">
    @foreach (string channel in Model)
    {
        @Html.Action("Thumbnail", "Channel", new { id = channel })
    }
</div>

@Html.Partial("_Pagination")